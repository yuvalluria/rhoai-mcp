# RHOAI MCP Server Configuration
# Copy this file to .env and configure as needed

# Authentication mode: auto | kubeconfig | token
# - auto: Try in-cluster first, fall back to kubeconfig
# - kubeconfig: Use kubeconfig file
# - token: Use explicit API server URL and token
RHOAI_MCP_AUTH_MODE=auto

# Kubeconfig settings (used when AUTH_MODE=kubeconfig or auto)
# RHOAI_MCP_KUBECONFIG_PATH=/path/to/kubeconfig
# RHOAI_MCP_KUBECONFIG_CONTEXT=my-context

# Token auth settings (used when AUTH_MODE=token)
# RHOAI_MCP_API_SERVER=https://api.cluster.example.com:6443
# RHOAI_MCP_API_TOKEN=sha256~xxxxx

# Default namespace for operations (optional)
# RHOAI_MCP_DEFAULT_NAMESPACE=my-project

# Transport mode: stdio | sse | streamable-http
RHOAI_MCP_TRANSPORT=stdio

# Server settings for HTTP transports
RHOAI_MCP_HOST=127.0.0.1
RHOAI_MCP_PORT=8000

# Safety settings
# Enable dangerous operations like delete (default: false)
RHOAI_MCP_ENABLE_DANGEROUS_OPERATIONS=false

# Read-only mode - disable all write operations (default: false)
RHOAI_MCP_READ_ONLY_MODE=false

# Logging level: DEBUG | INFO | WARNING | ERROR
RHOAI_MCP_LOG_LEVEL=INFO
<<<<<<< HEAD
=======

# -----------------------------------------------------------------------------
# NeuralNav/Opik branches (see README "Branches" section)
# -----------------------------------------------------------------------------
# single-tool-flow branch: Deployment-only (1 tool). Set only the URL (default is deployment_only).
# agent-prompt-optimization branch: Agent + prompt (3 tools). Set TOOL_BRANCH=agent_prompt and URL.
# -----------------------------------------------------------------------------
# Tool branch: deployment_only | agent_prompt
# - deployment_only: only get_deployment_recommendation (use on single-tool-flow)
# - agent_prompt: run_prompt_evaluation, run_prompt_optimization, get_agent_recommendation (use on agent-prompt-optimization)
# RHOAI_MCP_TOOL_BRANCH=deployment_only
# RHOAI_MCP_TOOL_BRANCH=agent_prompt

# NeuralNav / Opik service - base URL (required for both branches)
# In-cluster: http://backend.neuralnav.svc.cluster.local:8000
# Local: http://localhost:8000
# RHOAI_MCP_OPIK_SERVICE_URL=http://localhost:8000

# Run without Kubernetes (NeuralNav/Opik tools only; RHOAI cluster tools will fail until kubeconfig is set)
# RHOAI_MCP_SKIP_K8S_CONNECT=true

# Optional: Opik observability (A) - send successful MCP tool calls as traces to Opik
# When set, tools (per branch: get_deployment_recommendation or run_prompt_* + get_agent_recommendation) send a trace to Opik.
# Get an API key from Comet/Opik (https://www.comet.com); set these in your MCP server env
# (e.g. Cursor MCP config) when testing from Claude Code, then open the project in Opik to view traces.
# RHOAI_MCP_OPIK_TRACE_API_URL=https://www.comet.com/opik/api
# RHOAI_MCP_OPIK_TRACE_API_KEY=your_opik_api_key
# RHOAI_MCP_OPIK_TRACE_PROJECT=rhoai-mcp
>>>>>>> 3778abf (NeuralNav + Opik tool flow: natural-language recommendation)
